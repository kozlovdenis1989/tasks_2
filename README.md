# Tasks

Приложение на FastAPI для управления задачами.  

│  
├── app/     
│   ├── repository/          # Работа с бд  
│   ├── main.py              # Основной файл с FastAPI приложением  
│   ├── models/              # модели бд  
│   ├── servises/            # логика обработки маршрутов  
│   ├── schemas/             # Схемы для валидации данных  
│   └── routers/             # Роутеры для обработки запросов  
│                          
│── docker/                  
│   ├── docker-compose.yml   # Докер-компос  
│   └── Dockerfile           # Докер файл  
│   └── requirements.txt     # Список зависимостей  
│   └── .env                 # Файл с переменными окружения для докера  
│  
└── README   





## Запуск проекта в Docker

## Перед запуском:

Убедитесь, что у вас установлены Docker и Docker Compose. Вы можете установить их, следуя инструкциям на официальных страницах.
Измените настроки Postgress в .env 

### Стандартные команды

1. **Сборка и запуск контейнеров**:

   Чтобы собрать и запустить все сервисы, выполните:

   ```bash
   docker-compose up --build
   ```

2. **Остановка контейнеров**:

   Чтобы остановить и удалить контейнеры, выполните:

   ```bash
   docker-compose down --volumes
   ```

### Обращение к API

После успешного запуска сервер доступен по адресу:

```
http://localhost:8000/docs
```

Это документация Swagger, где вы можете тестировать API.

### Примечания

В данном рпоекте таблица бызы данных создается напрямую через sqlalchemy при запуске приложения, в дальнейшем можно подключить alembic.

Приложение и работа с базой данных реализованы синхронно.


## Стек технологий

- **FastAPI**: 
- **Sqlalchemy**: 
- **PostgreSQL**:
- **Docker**: 
